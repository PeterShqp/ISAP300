/*
 * NMPort.cpp
 *
 *  Created on: 2015年10月12日
 *      Author: Administrator
 */

#include "NMPort.h"

std::map<uint32, NMPort*> NMPort::NMPortMap;

NMPort::NMPort(RealPortBase& phyPort, NMChannel& channel) : Resource(phyPort.getUID()), ch(channel){

    upstreamUid = 0;
    memset( upstreamIP, 0, 4);
    memset( upstreamIpMask, 0 , 4);
    port = 0;
}

NMPort::~NMPort() {
    // TODO Auto-generated destructor stub
}


void NMPort::sendData(uint8* d, uint32 len) {
    ch.sendData(d, len);
}

bool NMPort::receivData(void) {
    return ch.receivData();
}

